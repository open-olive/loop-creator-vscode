/* eslint-disable @typescript-eslint/no-empty-function */
import { whisper } from '@oliveai/ldk';
import { write } from '@oliveai/ldk/dist/clipboard';
import { OcrLoop, writeWhisper, performOcr } from './screenExample';

jest.mock('@oliveai/ldk');

const writeWhisperSpy = jest.fn().mockImplementation(() => {});

jest.mock('./screenExample', () => ({
  writeWhisper: () => {
    writeWhisperSpy();
  },
}));

describe('Cursor Listener', () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  it('should call write whisper when the function is called', () => {
    writeWhisper('test', 'test');

    expect(writeWhisperSpy).toBeCalled();
  });

  // TODO: will handle once type is fixed

  it('should call performOCR', () => {
    performOcr();

    expect(screen.ocr).toBeCalled();
  })
});
